CREATE TABLE `quiz_state` (
user_id Uint64, 
question_index Uint64,
score Uint64,
PRIMARY KEY (`user_id`)
);

CREATE TABLE `quiz` (
question_id Uint64, 
question String,
options JSON
correct_option Uint64,
PRIMARY KEY (`question_id`)
);

COMMIT;